## Figure 6. The Phase 1 IND document landscape: before, during, and after submission

```mermaid
%%{init: {'theme':'base','themeVariables':{'fontSize':'11px','lineColor':'#6C757D'}}}%%
flowchart TB
    HUB["Repository LLM<br/>IND document generation"]:::accent
    subgraph BEFORE["Before submission"]
      direction TB
      B1["IND dossier (1571-11)"]:::input
      B2["Investigator's Brochure"]:::input
      B3["Clinical protocol"]:::input
      B4["Informed consent (ICF)"]:::input
    end
    subgraph DURING["During the trial"]
      direction TB
      D1["SAE / SUSAR 7 / 15-day"]:::proc
      D2["DSUR (annual)"]:::proc
      D3["Protocol amendments"]:::proc
      D4["Dose-escalation minutes"]:::proc
    end
    subgraph AFTER["After the trial"]
      direction TB
      A1["Tables, listings, figures"]:::goal
      A2["CSR (ICH E3)"]:::goal
      A3["Manuscripts / lay summaries"]:::goal
    end
    B1 --> HUB
    B2 --> HUB
    B3 --> HUB
    B4 --> HUB
    HUB --> D1
    HUB --> D2
    HUB --> D3
    HUB --> D4
    HUB --> A1
    A1 --> A2 --> A3
    classDef goal fill:#000000,stroke:#000000,stroke-width:1.5px,color:#FFFFFF
    classDef proc fill:#3F3F3F,stroke:#000000,stroke-width:1px,color:#FFFFFF
    classDef accent fill:#6C757D,stroke:#000000,stroke-width:1px,color:#FFFFFF
    classDef input fill:#ECECEC,stroke:#3F3F3F,stroke-width:1px,color:#000000
```

**Caption.** The large Phase 1 oncology document landscape around the IND, grouped
into before-submission inputs (the IND dossier, Investigator's Brochure, protocol,
and consent), during-trial process documents (7 and 15-day safety reports, the
annual DSUR, amendments, and dose-escalation minutes), and after-trial outputs
(tables / listings / figures, the ICH E3 clinical study report, and manuscripts /
lay summaries), all generated by the repository LLM around the central hub.

**Role in the IND.** Renders in the Introduction (§3.1) to place the IND in the
full document lifecycle the same method maintains.

**Source files.**
`trial-documents/final-paper/publication/sections/sec-03-methods.tex` (Figure 6,
the before / during / after landscape, adapted in context);
`trial-protocol/final-protocol/publication/sections/sec-10-oversight.tex`
(safety-report and DSUR cadence).
